#pragma once

#include <string_view>

namespace LearnVulkan {

	/**
	 * @brief Information to locate specific resources for the application.
	*/
	namespace ResourcePath {

		/**
		 * @brief Where all shader source code reside.
		*/
		constexpr inline std::string_view ShaderRoot = "${LV_SHADER_ROOT}";

		/**
		 * @brief Where assets for different purposes reside.
		*/
		constexpr inline std::string_view GeneralResourceRoot = "${LV_GENERAL_RESOURCE_ROOT}",
			HeightfieldResourceRoot = "${LV_HEIGHTFIELD_RESOURCE_ROOT}",
			SkyCubeMapResourceRoot = "${LV_SKY_CUBEMAP_RESOURCE_ROOT}";

	}

}